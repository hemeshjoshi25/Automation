
class SH(object):
    # Metric Dropdown
    MET_DROP = ['Total Item Spend (per Household)', 'Total Item Units (per Household)',
                'Total Item Trips (per Household)', 'Average Item Spend per Unit (per Household)',
                'Average Item Spend per Trip (per Household)', 'Average Item Units per Trip (per Household)',
                'Total Item Spend (per Trip)', 'Total Item Units (per Trip)', 'Average Item Spend per Unit (per Trip)']
    # Metric Dropdown [RV]
    MET_DROP_RV = ['Total Basket Spend (per Household)', 'Total Basket Trips (per Household)',
                   'Average Basket Spend per Trip (per Household)', 'Total Basket Units (per Household)',
                   'Average Basket Units per Trip (per Household)', 'Total Basket Spend (per Trip)',
                   'Total Basket Units (per Trip)']
    # Static Header
    STATIC_HEAD = [[None, '% HOUSEHOLDS', 'CUMULATIVE % HOUSEHOLDS', 'HOUSEHOLD COUNT'],
                   [None, '% TRIPS', 'CUMULATIVE % TRIPS', 'TRIP COUNT']]
